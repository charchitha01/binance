# binance
A Flask-based web application that connects to Binance Futures Testnet and allows users to place BUY and SELL market or limit orders through a simple web interface. It securely uses API keys, handles user input validation, logs requests and responses, and displays real-time order execution results.
